# Backpropagation Neural Network
Backpropagation neural network implementation for the 4-input AND operation, focusing on minimal architecture search and training stability analysis. The network is evaluated across multiple configurations to identify the smallest number of neurons and epochs needed for reliable performance.
